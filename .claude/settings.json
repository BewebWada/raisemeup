{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /root/.claude/projects/-var-www-raisemeup-jp-public-html/memory && git pull --quiet 2>&1 || true",
            "statusMessage": "Syncing project memory (git pull)..."
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /root/.claude/projects/-var-www-raisemeup-jp-public-html/memory && git add -A && (git diff --cached --quiet || git commit -q -m \"Auto-sync memory $(date -u +%Y-%m-%dT%H:%M:%SZ)\") && git pull --rebase --autostash --quiet && git push --quiet 2>&1 || true",
            "statusMessage": "Syncing project memory (git push)..."
          }
        ]
      }
    ]
  }
}
